import UserSettingsClient from '@/features/users/UserSettingsClient';

export const metadata = {
  title: 'User Settings',
};

export default function UserSettingsPage() {
  return <UserSettingsClient />;
}
