import SubscriptionClient from '@/features/organizations/SubscriptionClient';

export const metadata = {
  title: 'Subscription Management',
};

export default function SubscriptionPage() {
  return <SubscriptionClient />;
}
